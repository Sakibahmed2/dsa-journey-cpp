#include <bits/stdc++.h>
using namespace std;

int main()
{
    list<int> l = {1, 2, 3, 4, 5};

    // cout << l.back() << endl; // access the tail element
    
    // cout << l.front() << endl; // access the head element
    // cout  << *l.begin() << endl;

    // cout << *next(l.begin(), 2) << endl; // access the ith element



    return 0;
}