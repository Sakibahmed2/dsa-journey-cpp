#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, m, k;
        cin >> n >> m >> k;
        int total_sq = n * m;
        int ans;
        if(k <= n && k >= m)
        {
            ans = total_sq - n;
        } else if(k >  )
    }
     
    return 0;
}